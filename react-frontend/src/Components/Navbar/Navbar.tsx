import "./Navbar.css";

function Navbar() {
  return (
    <header className="header">
      <a href="/" className="logo">
        Logo
      </a>
      <nav className="navbar">
        <a href="/">Passwords</a>
        <a href="/">Passwords</a>
        <a href="/">Passwords</a>
        <a href="/">Passwords</a>
      </nav>
    </header>
  );
}

export default Navbar;
